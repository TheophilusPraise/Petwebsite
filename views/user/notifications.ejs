<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Notifications - PetCare</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="shortcut icon" href="/assets/Favicon.ico" type="image/jpeg">
  <script src="https://kit.fontawesome.com/cb8535f973.js" crossorigin="anonymous"></script>
</head>
<body>
  <header class="header">
    <a href="/dashboard/user" class="logo"><i class="fa-solid fa-paw"></i>Pet<span>Care</span></a>
    <nav class="navbar">
      <a href="/dashboard/user">Dashboard</a>
      <a href="/user/services">Services</a>
      <a href="/user/bookings">Bookings</a>
      <a href="/user/pets">My Pets</a>
      <a href="/user/notifications" class="active">Notifications</a>
      <a href="/auth/logout">Logout</a>
    </nav>
    <i id="menu-btn" class="fas fa-bars"></i>
  </header>

  <section style="padding: 3rem 0; background: #f8f9fa; min-height: 80vh;">
    <div style="max-width: 900px; margin: 0 auto;">
      <h2 style="
        color: var(--orange, #c88407);
        text-shadow: .4rem .4rem 0 rgba(0, 0, 0, .1);
        font-size: 2.5rem;
        margin-bottom: 2rem;
        letter-spacing: .1rem;
        font-weight: bold;
        text-transform: uppercase;
        text-align: center;
      ">Notifications</h2>

      <div id="notifications-container">
        <% if (notifications && notifications.length > 0) { %>
          <table style="width:100%; background:#fff; border-radius:.7rem; box-shadow:.5rem .5rem 0 rgba(22,160,133,.12); border:.2rem solid rgba(0,0,0,0.08);">
            <thead>
              <tr>
                <th style="padding:1rem; text-align:left;">Message</th>
                <th style="padding:1rem; text-align:left;">Date</th>
                <th style="padding:1rem; text-align:left;">Status</th>
              </tr>
            </thead>
            <tbody>
              <% notifications.forEach(function(note) { %>
                <tr>
                  <td style="padding:1rem;"><%= note.message %></td>
                  <td style="padding:1rem;"><%= new Date(note.created_at).toLocaleString() %></td>
                  <td style="padding:1rem;">
                    <% if (note.is_read) { %>
                      <span style="background:#27ae60; color:#fff; border-radius:1rem; padding:.3rem .8rem;">Read</span>
                    <% } else { %>
                      <span style="background:#f39c12; color:#fff; border-radius:1rem; padding:.3rem .8rem;">Unread</span>
                    <% } %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } else { %>
          <div class="no-notifications" style="
            background: var(--white, #fff);
            border-radius: .7rem;
            box-shadow: .5rem .5rem 0 rgba(22, 160, 133, .12);
            border: .2rem solid rgba(0,0,0,0.08);
            padding: 3rem 2rem;
            text-align: center;
          ">
            <i class="fa-solid fa-bell" style="font-size: 3.5rem; color: var(--orange, #c88407); background: #f3e8d9; border-radius: 50%; padding: 1.2rem;"></i>
            <h3 style="font-size: 1.4rem; color: var(--black, #1e1e1f); margin: 1rem 0 .5rem 0;">No Notifications</h3>
            <p style="color: var(--light-color, #666); font-size: 1.1rem;">
              You don't have any notifications yet.
            </p>
          </div>
        <% } %>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <section class="footer">
    <div class="footer-content">
      <h3><i class="fa-solid fa-paw"></i>Pet<span>Care</span></h3>
      <p>PetCare is a pet caring shop, where we provide facilities and various useful treatments. We offer Massage, a good ground, expert staff, good facilities and much more! Visit us anytime :). We are closed only on Thursday and our regular timings are 9:00AM to 10:00PM.</p>
      <ul class="socials">
        <li><a href="#"><i class="fa fa-facebook"></i></a></li>
        <li><a href="#"><i class="fa fa-twitter"></i></a></li>
        <li><a href="#"><i class="fa-brands fa-google-plus-g"></i></a></li>
        <li><a href="#"><i class="fa fa-youtube"></i></a></li>
        <li><a href="#"><i class="fa fa-linkedin-square"></i></a></li>
      </ul>
    </div>
  </section>
  <section class="footer-bottom">
    <p>copyright &copy;<span id="currentYear"></span> <a href="#">Shode Miracle</a> - Pet Care All rights reserved.</p>
  </section>
  <script>
    document.getElementById('currentYear').textContent = new Date().getFullYear();
  </script>
</body>
</html>
